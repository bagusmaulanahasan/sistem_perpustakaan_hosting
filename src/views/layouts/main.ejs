<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %> | Perpus Online</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
        />
    </head>
    <script></script>
    <body class="bg-gray-100">
        <div class="none md:flex h-screen">
            <aside class="w-64 bg-gray-800 text-white flex-shrink-0">
                <div class="p-4 text-2xl font-bold flex items-center gap-2">
                    <img
                        src="/logo-perpuslan.png"
                        alt=""
                        class="w-12 h-12 rounded-full"
                    />
                    <span>PerpusOnline</span>
                </div>
                <nav class="mt-6">
                    <% if (user && user.role === 'admin') { %>
                    <h3
                        class="px-4 text-xs uppercase text-gray-400 font-semibold tracking-wider"
                    >
                        Admin Menu
                    </h3>
                    <a
                        href="/katalog"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Katalog Buku' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-book mr-2"></i> Katalog Buku</a
                    >
                    <a
                        href="/admin/books"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Manajemen Buku' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-database mr-2"></i> Data Buku</a
                    >
                    <a
                        href="/admin/categories"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Manajemen Kategori' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-tags mr-2"></i> Data Kategori</a
                    >
                    <a
                        href="/admin/borrowings"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Buku Sedang Dipinjam' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-book-open-reader mr-2"></i> Sedang
                        Dipinjam</a
                    >
                    <a
                        href="/admin/history"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 <%= title == 'Riwayat Peminjaman' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-clock-rotate-left mr-2"></i> Riwayat
                        Peminjaman</a
                    >
                    <% } else { %>
                    <h3
                        class="px-4 text-xs uppercase text-gray-400 font-semibold tracking-wider"
                    >
                        Anggota Menu
                    </h3>
                    <a
                        href="/katalog"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Katalog Buku' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-book mr-2"></i> Katalog Buku</a
                    >
                    <a
                        href="/wishlist"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Wishlist Saya' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-heart mr-2"></i> Wishlist</a
                    >
                    <a
                        href="/dipinjam"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded  <%= title == 'Buku yang Sedang Dipinjam' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-book-reader mr-2"></i> Buku
                        Dipinjam</a
                    >
                    <a
                        href="/riwayat"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded <%= title == 'Riwayat Peminjaman' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-clock-rotate-left mr-2"></i> Riwayat
                        Pinjaman</a
                    >
                    <a
                        href="/profil"
                        class="block px-4 py-2 mt-2 text-sm hover:bg-gray-700 rounded border-t border-gray-700 pt-4  <%= title == 'Profil Saya' ? 'bg-gray-700' : '' %>"
                        ><i class="fas fa-user mr-2"></i> Profil Saya</a
                    >
                    <% } %>
                </nav>
                <div class="absolute bottom-0 w-64 p-4">
                    <a
                        href="/logout"
                        class="block w-full text-center px-4 py-2 text-sm bg-red-600 hover:bg-red-700 rounded"
                        >Logout</a
                    >
                </div>
            </aside>

            <main class="flex-1 flex flex-col">
                <header
                    class="bg-white shadow-sm p-4 flex justify-between items-center sticky top-0 z-50"
                >
                    <h1 class="text-xl font-semibold text-gray-700">
                        <%= title %>
                    </h1>
                    <div class="text-right">
                        <p class="font-medium"><%= user.username %></p>
                        <p class="font-medium text-slate-600 text-sm">
                            <%= user.email %>
                        </p>
                    </div>
                </header>
                <div
                    class="flex-1 px-6 <%= title === 'Katalog Buku' ? '' : 'py-6' %> overflow-y-auto"
                >
                    <%- body %>
                </div>
            </main>
        </div>
    </body>
</html>
